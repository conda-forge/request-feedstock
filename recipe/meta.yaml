{% set name = "request" %}
{% set version = "2.88.1" %}
{% set sha256 = "6506ec33a673f6a5542a950be65b02b23c8e8cda21847c19ee67dee4fdc7adc4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/request/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: generic
  script: npm install -g .

requirements:
  build:
    - nodejs
  
test:
  commands:
    - npm list -g request
  requires:
    - nodejs
   

about:
  home: https://github.com/request/request
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: 'Simplified HTTP request client.'
  description: |
    Simplified HTTP request client. 
  doc_url: https://github.com/request/request
  dev_url: https://github.com/request/request

extra:
  recipe-maintainers:
    - cshaley
    - sannykr
